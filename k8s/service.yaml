apiVersion: v1
kind: Service
metadata:
  name: network-mcp
  labels:
    app: network-mcp
spec:
  type: ClusterIP
  selector:
    app: network-mcp
  ports:
    - port: 8000
      targetPort: 8000
      protocol: TCP
      name: http
